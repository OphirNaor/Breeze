import { Box } from '@mui/material';
import { Link } from 'react-router-dom';

const NavBar = () => {
    return (
        <Box>
            <Link to='/'>Home</Link>
        </Box>
    )
}

export default NavBar